#ifndef INVASIONOVERLAY_H
#define INVASIONOVERLAY_H

#include <QQuickView>
class InvasionOverlay : public QQuickView
{
    Q_OBJECT

public:
    explicit InvasionOverlay(QWindow *parent = nullptr);

protected:

private slots:
};

#endif
